<map
  id="map"
  longitude="{{lng}}"
  latitude="{{lat}}"
  scale="13"
  circles="{{circles}}"
  markers="{{markers}}"
  bindmarkertap="markertap"
  show-location
  enable-scroll="{{false}}"
  style="width: 100%; height: 400px;"
>
<cover-view class="hide-all-class"></cover-view>
</map>
